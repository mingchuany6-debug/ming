name: Build HighDe PC V2.2.1 EXE

on:
  workflow_dispatch:
  push:
    paths:
      - 'highde-pc-v221/**'
      - '.github/workflows/highde-pc-v221.yml'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --upgrade pip pandas openpyxl pyinstaller
      - name: Build EXE
        run: pyinstaller --noconfirm --clean --onefile --windowed --name HighDe_Hotel_PC_V2.2.1 highde-pc-v221/AMap_PC_Bridge.py
      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: HighDe_Hotel_PC_V2.2.1
          path: dist/HighDe_Hotel_PC_V2.2.1.exe
          if-no-files-found: error
